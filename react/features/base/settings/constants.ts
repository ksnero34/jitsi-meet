import SORTINGORDERS_RESOURCES from '../../../../sorting/sortingOrder.json';

/**
 * The default server URL to open if no other was specified.
 */
export const DEFAULT_SERVER_URL = 'https://meet.jit.si';

export const SORTINGORDERS: Array<string> = Object.values(SORTINGORDERS_RESOURCES);